#include <stdio.h>

int main() {
  int blue_balls[5]; // массив из 5 синих шариков
  int size = sizeof(blue_balls) / sizeof(blue_balls[0]); // вычисление размера массива
  printf("В коробке blue_balls помещается %d шариков.\n", size); // вывод результата
  return 0;
